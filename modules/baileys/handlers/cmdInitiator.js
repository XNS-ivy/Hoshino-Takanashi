import { configDotenv } from "dotenv"
configDotenv()
/**
 * On Going Project
 * @param {*} msg 
 * @returns 
 */
export default async function initCommand(msg = {}) {
    const isHitCommand = {}
    if (isHitCommand) {
        return {
            msg,
            commandName,
        }
    } else {
        return false
    }
}